pub mod shipping_provider {
    pub mod presenter;
    pub mod repository_postgres;
    pub mod usecase;
}

pub mod shipping {
    pub mod presenter;
    pub mod repository_postgres;
    pub mod usecase;
}